const {getMetricsAsset} =require('./assets')
const {getFloorPrice} =require('./floorprice')
const {getMarketcap} =require('./marketcap')

module.exports={
    getMetricsAsset,
    getFloorPrice,
    getMarketcap
}